# ============================================
# ABSA University - Dokploy Environment Variables (Self-Hosted)
# ============================================
# Copia SOLO estos valores en la interfaz de Dokploy
# NO necesitas variables de Manus en producción self-hosted

# ============================================
# BASE DE DATOS (MySQL)
# ============================================
DB_ROOT_PASSWORD=absa_root_secure_change_me_in_production
DB_NAME=absa_lms
DB_USER=absa_user
DB_PASSWORD=absa_user_secure_change_me_in_production
DB_PORT=3306
DB_HOST=db
DATABASE_URL=mysql://absa_user:absa_user_secure_change_me_in_production@db:3306/absa_lms

# ============================================
# APLICACIÓN
# ============================================
NODE_ENV=production
APP_PORT=8082
VITE_APP_TITLE=ABSA UNIVERSITY
VITE_APP_LOGO=/logo-absa.png

# ============================================
# SEGURIDAD - JWT
# ============================================
# Genera un JWT_SECRET seguro: openssl rand -hex 32
JWT_SECRET=2f83db95d74a05d15482eaf872edfc76

# ============================================
# QDRANT - Vector Database para RAG
# ============================================
QDRANT_URL=http://qdrant:6333
QDRANT_API_KEY=

# ============================================
# OPENAI - Para embeddings y LLM
# ============================================
# Obtén tu API key en https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your_openai_api_key_here

# ============================================
# NOTAS IMPORTANTES
# ============================================
# 1. NO necesitas VITE_APP_ID, OAUTH_SERVER_URL, VITE_OAUTH_PORTAL_URL
#    Estas variables son SOLO para desarrollo con Manus OAuth
#
# 2. NO necesitas BUILT_IN_FORGE_API_* en producción self-hosted
#    Usas Ollama, Qdrant y MinIO locales en su lugar
#
# 3. Cambia todos los valores "change_me" por valores seguros en producción
#
# 4. Para generar JWT_SECRET seguro:
#    openssl rand -hex 32
#
# 5. Las credenciales de BD deben coincidir en docker-compose.self-hosted.yml
